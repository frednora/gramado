
 net/prot/core/

Contains the foundational protocols that everything else depends on:
 + IP (network layer)
 + ARP (address resolution)

These are the “core” because higher‑level protocols can’t function without them.
