# dungeon

This is the main ring 0 loadable kernel module.

```
#todo:
We gotta find a good purpose for this one module that we have?

Good candidates:
+ system registry manager
  (not latency‑critical, Extensibility, Isolation, Configurability)
+ ?
+ ?

```
